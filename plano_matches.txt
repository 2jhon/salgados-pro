[initSystem]);\n\n  // Sincronização em tempo real do perfil do usuário logado (Detecta ativação de plano PRO instantaneamente)\n  useEffect(() => {\n    if (!currentUser?.id) return;\n\n    const userChan
('logged_user'); }\n    }\n  }, [currentUser, loadWorkspaceData]);\n\n  // Segurança e Automação de Plano\n  useEffect(() => {\n    if (!currentUser || currentUser.role !== 'OWNER' || !companyProfile) retu
 vitrine está desligada, liga automaticamente\n      console.log('[AUTO-ACTIVATE] Ativando vitrine: Plano PRO detectado.');\n      saveProfile({ ...companyProfile, active: true }).then(p => p && setCompany
imer = setTimeout(() => {\n         console.log('[SECURITY] Desativando vitrine: Usuário não possui Plano PRO ativo.');\n         saveProfile({ ...companyProfile, active: false }).then(p => p && setCompany
[initSystem]);\n\n  // Sincronização em tempo real do perfil do usuário logado (Detecta ativação de plano PRO instantaneamente)\n  useEffect(() => {\n    if (!currentUser?.id) return;\n\n    const userChan
('logged_user'); }\n    }\n  }, [currentUser, loadWorkspaceData]);\n\n  // Segurança e Automação de Plano\n  useEffect(() => {\n    if (!currentUser || currentUser.role !== 'OWNER' || !companyProfile) retu
 vitrine está desligada, liga automaticamente\n      console.log('[AUTO-ACTIVATE] Ativando vitrine: Plano PRO detectado.');\n      saveProfile({ ...companyProfile, active: true }).then(p => p && setCompany
imer = setTimeout(() => {\n         console.log('[SECURITY] Desativando vitrine: Usuário não possui Plano PRO ativo.');\n         saveProfile({ ...companyProfile, active: false }).then(p => p && setCompany
[initSystem]);\n\n  // Sincronização em tempo real do perfil do usuário logado (Detecta ativação de plano PRO instantaneamente)\n  useEffect(() => {\n    if (!currentUser?.id) return;\n\n    const userChan
('logged_user'); }\n    }\n  }, [currentUser, loadWorkspaceData]);\n\n  // Segurança e Automação de Plano\n  useEffect(() => {\n    if (!currentUser || currentUser.role !== 'OWNER' || !companyProfile) retu
 vitrine está desligada, liga automaticamente\n      console.log('[AUTO-ACTIVATE] Ativando vitrine: Plano PRO detectado.');\n      saveProfile({ ...companyProfile, active: true }).then(p => p && setCompany
imer = setTimeout(() => {\n         console.log('[SECURITY] Desativando vitrine: Usuário não possui Plano PRO ativo.');\n         saveProfile({ ...companyProfile, active: false }).then(p => p && setCompany
      "text": "na area de de super admin, as empresas que nao aderiram a nenhum plano, esta sem cor, na minha empresa, pastel br, eu to com o plano pro ativo, esta aparecendo amarelo e 
esar em verde, mas esta sem o time, mesmo eu tendo um anuncio ativo. ja a empresa test, ja venceu o plano pro, e venceu o anuncio, foi interrompido corretamente, mas aqui no painel ainda continua aceso com
      "text": "Para corrigir a inconsistência visual onde empresas com planos vencidos continuam \"acesas\" (coloridas) e empresas sem data de expiração definida aparecem ativa
renderização no painel `SuperAdmin`.\n\nA mudança principal será verificar não apenas a \"flag\" do plano (ex: `hasProPlan`), mas também se a data de expiração (`proExpiresAt`) é válida e está no futuro. I
eq('role', 'OWNER');\n      \n      if (!error) {\n        // ATIVAÇÃO AUTOMÁTICA DA VITRINE SE FOR PLANO PRO\n        if (planToApprove.field === 'hasProPlan') {\n          console.log(\"Ativando vitrine 
       } : c));\n        setPlanToApprove(null);\n      }\n    } catch (e) { alert(\"Erro ao ativar plano.\"); }\n    finally { setIsSaving(false); }\n  };\n\n  const executeEndPlan = async () => {\n    if
at' }\n      };\n\n      const fields = dbFields[planToManage.field];\n      \n      // 1. Remove o plano do usuário\n      const { error } = await supabase.from('users').update({ \n        [fields.f]: fal
pany.workspaceId).eq('role', 'OWNER');\n      \n      if (error) throw error;\n\n      // 2. SE FOR PLANO PRO: Força desativação da vitrine imediatamente\n      if (planToManage.field === 'hasProPlan') {\n
 setShowEndConfirm(false);\n      \n    } catch (e: any) { \n      console.error(\"Erro ao encerrar plano:\", e);\n      alert(\"Erro ao encerrar plano: \" + (e.message || \"Erro desconhecido\"));\n    } f
se().includes(searchTerm.toLowerCase()));\n\n  // Função auxiliar para lidar com clique no botão do plano\n  const handlePlanClick = (company: GlobalCompany, field: 'hasProPlan' | 'isAdFree' | 'isAdvertise
ppingCart size={18} />\n                        <span className=\"text-[7px] font-black uppercase\">Plano Pro</span>\n                        {isProActive && <AdTimer expiresAt={c.proExpiresAt!} lightMode 
><h3 className=\"text-xl font-black text-slate-800 uppercase\">{adToApprove ? 'Ativar Ad' : 'Ativar Plano'}</h3><p className=\"text-[9px] font-black text-blue-600 uppercase mt-1\">Selecione a Duração</p></
 a remover o acesso de <strong className=\"text-slate-800\">{planToManage.company.name}</strong> ao plano selecionado. Esta ação é imediata.\n              </p>\n              \n              <div classNam
: "eu ativei um anuncio, na area anuncio esta aparecendo corretamente, mas nessa aba onde mostra os planos, a parte anunciante nao acendeu com o time automaticamente, ja que aprovei o anuncio, deveria acen
eq('role', 'OWNER');\n      \n      if (!error) {\n        // ATIVAÇÃO AUTOMÁTICA DA VITRINE SE FOR PLANO PRO\n        if (planToApprove.field === 'hasProPlan') {\n          console.log(\"Ativando vitrine 
       } : c));\n        setPlanToApprove(null);\n      }\n    } catch (e) { alert(\"Erro ao ativar plano.\"); }\n    finally { setIsSaving(false); }\n  };\n\n  const executeEndPlan = async () => {\n    if
at' }\n      };\n\n      const fields = dbFields[planToManage.field];\n      \n      // 1. Remove o plano do usuário\n      const { error } = await supabase.from('users').update({ \n        [fields.f]: fal
pany.workspaceId).eq('role', 'OWNER');\n      \n      if (error) throw error;\n\n      // 2. SE FOR PLANO PRO: Força desativação da vitrine imediatamente\n      if (planToManage.field === 'hasProPlan') {\n
 setShowEndConfirm(false);\n      \n    } catch (e: any) { \n      console.error(\"Erro ao encerrar plano:\", e);\n      alert(\"Erro ao encerrar plano: \" + (e.message || \"Erro desconhecido\"));\n    } f
se().includes(searchTerm.toLowerCase()));\n\n  // Função auxiliar para lidar com clique no botão do plano\n  const handlePlanClick = (company: GlobalCompany, field: 'hasProPlan' | 'isAdFree' | 'isAdvertise
ppingCart size={18} />\n                        <span className=\"text-[7px] font-black uppercase\">Plano Pro</span>\n                        {isProActive && <AdTimer expiresAt={c.proExpiresAt!} lightMode 
><h3 className=\"text-xl font-black text-slate-800 uppercase\">{adToApprove ? 'Ativar Ad' : 'Ativar Plano'}</h3><p className=\"text-[9px] font-black text-blue-600 uppercase mt-1\">Selecione a Duração</p></
 a remover o acesso de <strong className=\"text-slate-800\">{planToManage.company.name}</strong> ao plano selecionado. Esta ação é imediata.\n              </p>\n              \n              <div classNam
[initSystem]);\n\n  // Sincronização em tempo real do perfil do usuário logado (Detecta ativação de plano PRO instantaneamente)\n  useEffect(() => {\n    if (!currentUser?.id) return;\n\n    const userChan
('logged_user'); }\n    }\n  }, [currentUser, loadWorkspaceData]);\n\n  // Segurança e Automação de Plano\n  useEffect(() => {\n    if (!currentUser || currentUser.role !== 'OWNER' || !companyProfile) retu
 vitrine está desligada, liga automaticamente\n      console.log('[AUTO-ACTIVATE] Ativando vitrine: Plano PRO detectado.');\n      saveProfile({ ...companyProfile, active: true }).then(p => p && setCompany
imer = setTimeout(() => {\n         console.log('[SECURITY] Desativando vitrine: Usuário não possui Plano PRO ativo.');\n         saveProfile({ ...companyProfile, active: false }).then(p => p && setCompany
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>(() => {\n    const pending = localStorage.getItem('settings_pending_tab');\n    if (pending) {\n
uto no-scrollbar\">\n        {['ESTRUTURA', 'CLIENTES', 'EQUIPE', 'VITRINE', 'ANUNCIO', 'SISTEMA', 'PLANOS'].map(tab => (\n          <button key={tab} onClick={() => handleTabClick(tab)} className={`flex-1
      </div>\n             </div>\n           )}\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
     <div className=\"relative z-10\">\n                 <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n                 <div className=\"flex items-center gap-3 mb-6\">\n                    
black text-sm uppercase flex items-center gap-2\"><Crown size={16} className=\"text-yellow-400\" /> Plano Pro</h4>\n                       <p className=\"text-[9px] font-bold opacity-70 uppercase mt-1\">Ge
     href={`https://wa.me/55${supportPhone}?text=${encodeURIComponent(\"Olá! Gostaria de assinar um plano no Salgados Pro.\")}`}\n                target=\"_blank\"\n                className=\"mt-6 w-full 
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>(() => {\n    const pending = localStorage.getItem('settings_pending_tab');\n    if (pending) {\n
uto no-scrollbar\">\n        {['ESTRUTURA', 'CLIENTES', 'EQUIPE', 'VITRINE', 'ANUNCIO', 'SISTEMA', 'PLANOS'].map(tab => (\n          <button key={tab} onClick={() => handleTabClick(tab)} className={`flex-1
      </div>\n             </div>\n           )}\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
     <div className=\"relative z-10\">\n                 <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n                 <div className=\"flex items-center gap-3 mb-6\">\n                    
black text-sm uppercase flex items-center gap-2\"><Crown size={16} className=\"text-yellow-400\" /> Plano Pro</h4>\n                       <p className=\"text-[9px] font-bold opacity-70 uppercase mt-1\">Ge
     href={`https://wa.me/55${supportPhone}?text=${encodeURIComponent(\"Olá! Gostaria de assinar um plano no Salgados Pro.\")}`}\n                target=\"_blank\"\n                className=\"mt-6 w-full 
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>(() => {\n    const pending = localStorage.getItem('settings_pending_tab');\n    if (pending) {\n
uto no-scrollbar\">\n        {['ESTRUTURA', 'CLIENTES', 'EQUIPE', 'VITRINE', 'ANUNCIO', 'SISTEMA', 'PLANOS'].map(tab => (\n          <button key={tab} onClick={() => handleTabClick(tab)} className={`flex-1
      </div>\n             </div>\n           )}\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
     <div className=\"relative z-10\">\n                 <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n                 <div className=\"flex items-center gap-3 mb-6\">\n                    
black text-sm uppercase flex items-center gap-2\"><Crown size={16} className=\"text-yellow-400\" /> Plano Pro</h4>\n                       <p className=\"text-[9px] font-bold opacity-70 uppercase mt-1\">Ge
     href={`https://wa.me/55${supportPhone}?text=${encodeURIComponent(\"Olá! Gostaria de assinar um plano no Salgados Pro.\")}`}\n                target=\"_blank\"\n                className=\"mt-6 w-full 
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>(() => {\n    const pending = localStorage.getItem('settings_pending_tab');\n    if (pending) {\n
uto no-scrollbar\">\n        {['ESTRUTURA', 'CLIENTES', 'EQUIPE', 'VITRINE', 'ANUNCIO', 'SISTEMA', 'PLANOS'].map(tab => (\n          <button key={tab} onClick={() => handleTabClick(tab)} className={`flex-1
      </div>\n             </div>\n           )}\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
     <div className=\"relative z-10\">\n                 <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n                 <div className=\"flex items-center gap-3 mb-6\">\n                    
black text-sm uppercase flex items-center gap-2\"><Crown size={16} className=\"text-yellow-400\" /> Plano Pro</h4>\n                       <p className=\"text-[9px] font-bold opacity-70 uppercase mt-1\">Ge
     href={`https://wa.me/55${supportPhone}?text=${encodeURIComponent(\"Olá! Gostaria de assinar um plano no Salgados Pro.\")}`}\n                target=\"_blank\"\n                className=\"mt-6 w-full 
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>(() => {\n    const pending = localStorage.getItem('settings_pending_tab');\n    if (pending) {\n
uto no-scrollbar\">\n        {['ESTRUTURA', 'CLIENTES', 'EQUIPE', 'VITRINE', 'ANUNCIO', 'SISTEMA', 'PLANOS'].map(tab => (\n          <button key={tab} onClick={() => handleTabClick(tab)} className={`flex-1
      </div>\n             </div>\n           )}\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
     <div className=\"relative z-10\">\n                 <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n                 <div className=\"flex items-center gap-3 mb-6\">\n                    
black text-sm uppercase flex items-center gap-2\"><Crown size={16} className=\"text-yellow-400\" /> Plano Pro</h4>\n                       <p className=\"text-[9px] font-bold opacity-70 uppercase mt-1\">Ge
     href={`https://wa.me/55${supportPhone}?text=${encodeURIComponent(\"Olá! Gostaria de assinar um plano no Salgados Pro.\")}`}\n                target=\"_blank\"\n                className=\"mt-6 w-full 
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>(() => {\n    const pending = localStorage.getItem('settings_pending_tab');\n    if (pending) {\n
uto no-scrollbar\">\n        {['ESTRUTURA', 'CLIENTES', 'EQUIPE', 'VITRINE', 'ANUNCIO', 'SISTEMA', 'PLANOS'].map(tab => (\n          <button key={tab} onClick={() => handleTabClick(tab)} className={`flex-1
      </div>\n             </div>\n           )}\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
     <div className=\"relative z-10\">\n                 <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n                 <div className=\"flex items-center gap-3 mb-6\">\n                    
black text-sm uppercase flex items-center gap-2\"><Crown size={16} className=\"text-yellow-400\" /> Plano Pro</h4>\n                       <p className=\"text-[9px] font-bold opacity-70 uppercase mt-1\">Ge
     href={`https://wa.me/55${supportPhone}?text=${encodeURIComponent(\"Olá! Gostaria de assinar um plano no Salgados Pro.\")}`}\n                target=\"_blank\"\n                className=\"mt-6 w-full 
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>(() => {\n    const pending = localStorage.getItem('settings_pending_tab');\n    if (pending) {\n
uto no-scrollbar\">\n        {['ESTRUTURA', 'CLIENTES', 'EQUIPE', 'VITRINE', 'ANUNCIO', 'SISTEMA', 'PLANOS'].map(tab => (\n          <button key={tab} onClick={() => handleTabClick(tab)} className={`flex-1
      </div>\n             </div>\n           )}\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
     <div className=\"relative z-10\">\n                 <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n                 <div className=\"flex items-center gap-3 mb-6\">\n                    
black text-sm uppercase flex items-center gap-2\"><Crown size={16} className=\"text-yellow-400\" /> Plano Pro</h4>\n                       <p className=\"text-[9px] font-bold opacity-70 uppercase mt-1\">Ge
     href={`https://wa.me/55${supportPhone}?text=${encodeURIComponent(\"Olá! Gostaria de assinar um plano no Salgados Pro.\")}`}\n                target=\"_blank\"\n                className=\"mt-6 w-full 
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>(() => {\n    const pending = localStorage.getItem('settings_pending_tab');\n    if (pending) {\n
uto no-scrollbar\">\n        {['ESTRUTURA', 'CLIENTES', 'EQUIPE', 'VITRINE', 'ANUNCIO', 'SISTEMA', 'PLANOS'].map(tab => (\n          <button key={tab} onClick={() => handleTabClick(tab)} className={`flex-1
      </div>\n             </div>\n           )}\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
     <div className=\"relative z-10\">\n                 <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n                 <div className=\"flex items-center gap-3 mb-6\">\n                    
black text-sm uppercase flex items-center gap-2\"><Crown size={16} className=\"text-yellow-400\" /> Plano Pro</h4>\n                       <p className=\"text-[9px] font-bold opacity-70 uppercase mt-1\">Ge
     href={`https://wa.me/55${supportPhone}?text=${encodeURIComponent(\"Olá! Gostaria de assinar um plano no Salgados Pro.\")}`}\n                target=\"_blank\"\n                className=\"mt-6 w-full 
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>(() => {\n    const pending = localStorage.getItem('settings_pending_tab');\n    if (pending) {\n
uto no-scrollbar\">\n        {['ESTRUTURA', 'CLIENTES', 'EQUIPE', 'VITRINE', 'ANUNCIO', 'SISTEMA', 'PLANOS'].map(tab => (\n          <button key={tab} onClick={() => handleTabClick(tab)} className={`flex-1
      </div>\n             </div>\n           )}\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
     <div className=\"relative z-10\">\n                 <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n                 <div className=\"flex items-center gap-3 mb-6\">\n                    
black text-sm uppercase flex items-center gap-2\"><Crown size={16} className=\"text-yellow-400\" /> Plano Pro</h4>\n                       <p className=\"text-[9px] font-bold opacity-70 uppercase mt-1\">Ge
     href={`https://wa.me/55${supportPhone}?text=${encodeURIComponent(\"Olá! Gostaria de assinar um plano no Salgados Pro.\")}`}\n                target=\"_blank\"\n                className=\"mt-6 w-full 
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>(() => {\n    const pending = localStorage.getItem('settings_pending_tab');\n    if (pending) {\n
uto no-scrollbar\">\n        {['ESTRUTURA', 'CLIENTES', 'EQUIPE', 'VITRINE', 'ANUNCIO', 'SISTEMA', 'PLANOS'].map(tab => (\n          <button key={tab} onClick={() => handleTabClick(tab)} className={`flex-1
      </div>\n             </div>\n           )}\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
     <div className=\"relative z-10\">\n                 <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n                 <div className=\"flex items-center gap-3 mb-6\">\n                    
black text-sm uppercase flex items-center gap-2\"><Crown size={16} className=\"text-yellow-400\" /> Plano Pro</h4>\n                       <p className=\"text-[9px] font-bold opacity-70 uppercase mt-1\">Ge
     href={`https://wa.me/55${supportPhone}?text=${encodeURIComponent(\"Olá! Gostaria de assinar um plano no Salgados Pro.\")}`}\n                target=\"_blank\"\n                className=\"mt-6 w-full 
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>(() => {\n    const pending = localStorage.getItem('settings_pending_tab');\n    if (pending) {\n
uto no-scrollbar\">\n        {['ESTRUTURA', 'CLIENTES', 'EQUIPE', 'VITRINE', 'ANUNCIO', 'SISTEMA', 'PLANOS'].map(tab => (\n          <button key={tab} onClick={() => handleTabClick(tab)} className={`flex-1
      </div>\n             </div>\n           )}\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
     <div className=\"relative z-10\">\n                 <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n                 <div className=\"flex items-center gap-3 mb-6\">\n                    
black text-sm uppercase flex items-center gap-2\"><Crown size={16} className=\"text-yellow-400\" /> Plano Pro</h4>\n                       <p className=\"text-[9px] font-bold opacity-70 uppercase mt-1\">Ge
     href={`https://wa.me/55${supportPhone}?text=${encodeURIComponent(\"Olá! Gostaria de assinar um plano no Salgados Pro.\")}`}\n                target=\"_blank\"\n                className=\"mt-6 w-full 
resas (contas a pagar compartilhadas) e geração automática de despesas ao quitar essas contas.\n\nO plano de implementação consiste em:\n1.  **Atualizar Tipagem (`types.ts`)**: Adicionar o campo `customerP
('logged_user'); }\n    }\n  }, [currentUser, loadWorkspaceData]);\n\n  // Segurança e Automação de Plano\n  useEffect(() => {\n    if (!currentUser || currentUser.role !== 'OWNER' || !companyProfile) retu
panyProfile.active) {\n      // AUTO-ATIVAÇÃO\n      console.log('[AUTO-ACTIVATE] Ativando vitrine: Plano PRO detectado.');\n      saveProfile({ ...companyProfile, active: true }).then(p => p && setCompany
imer = setTimeout(() => {\n         console.log('[SECURITY] Desativando vitrine: Usuário não possui Plano PRO ativo.');\n         saveProfile({ ...companyProfile, active: false }).then(p => p && setCompany
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
('logged_user'); }\n    }\n  }, [currentUser, loadWorkspaceData]);\n\n  // Segurança e Automação de Plano\n  useEffect(() => {\n    if (!currentUser || currentUser.role !== 'OWNER' || !companyProfile) retu
panyProfile.active) {\n      // AUTO-ATIVAÇÃO\n      console.log('[AUTO-ACTIVATE] Ativando vitrine: Plano PRO detectado.');\n      saveProfile({ ...companyProfile, active: true }).then(p => p && setCompany
imer = setTimeout(() => {\n         console.log('[SECURITY] Desativando vitrine: Usuário não possui Plano PRO ativo.');\n         saveProfile({ ...companyProfile, active: false }).then(p => p && setCompany
guns campos de informaçao. sumiu a aba anuncios, na arae sistema foi tudo simplificado, sumiu a aba planos",
epancy between the `activeTab` state definition and the rendered tab list. The 'ANUNCIO' (Ads) and 'PLANOS' (Plans) tabs are missing from the UI, despite being included in the state's type definition. I ne
pecific Changes**\n\nI'm currently updating `components/Settings.tsx` to include the 'ANÚNCIO' and 'PLANOS' tabs in the tabs list. I'm adding edit buttons to both the Structure and Team tabs, with modal lo
da a aba **Anúncios** com listagem, criação e edição de campanhas de marketing internas.\n4.  **Aba Planos:** Restaurada a aba **Planos** para gestão da assinatura (Pro, Sem Anúncios, etc), mostrando statu
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
         ))\n              )}\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-white p-8 rounded-[3r
e-500 to-pink-500\" />\n              <h3 className=\"text-2xl font-black text-slate-800 mb-2\">Seu Plano Atual</h3>\n              <div className=\"inline-block px-4 py-1 bg-slate-100 rounded-full text-[1
             onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Olá, quero assinar o Plano PRO!`, '_blank')}\n                      className=\"w-full py-4 bg-gradient-to-r from-indigo-600 t
the discrepancy regarding the tabs. My current analysis of `Settings.tsx` shows that 'ANUNCIO' and 'PLANOS' are present, along with the edit buttons in both 'ESTRUTURA' and 'EQUIPE'. The user reported they
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
         ))\n              )}\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-white p-8 rounded-[3r
e-500 to-pink-500\" />\n              <h3 className=\"text-2xl font-black text-slate-800 mb-2\">Seu Plano Atual</h3>\n              <div className=\"inline-block px-4 py-1 bg-slate-100 rounded-full text-[1
             onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Olá, quero assinar o Plano PRO!`, '_blank')}\n                      className=\"w-full py-4 bg-gradient-to-r from-indigo-600 t
 given the previously requested features like \"Colaborador,\" \"Gerenciar Aba,\" \"Sua Equipe,\" \"Plano Atual,\" download history, clear history, and sound selection.\n\n\n**Examining Tab Navigation Logi
nto the \"ANUNCIANTE\" and \"ASSINAR OU RENOVAR\" sections. I'm focusing on the plan details like \"PLANO PRO R$ 34,90\" and \"REMOVER ANÚNCIOS R$ 9,90\", and the corresponding layout in the light card of 
\"Seu Plano Atual\". \"SUA EQUIPE\" requires more aesthetic adjustments. Specifically, I'm analyzing the differ
ted.\n\n\n**Implementing UI Enhancements**\n\nI've now incorporated the dark card styling for the \"Plano Atual\" and the orange header for the \"Divulgue Aqui\" sections. I'm verifying the visual consiste
d the visual style of the \"Current Plan\" card to match the dark theme shown in your screenshot (\"Plano Atual\").\n3.  **Ads Tab Redesign**: I've styled the top section to look like the \"Divulgue Aqui\"
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
    </div>\n              ))}\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\">\n                 <span className={`p
                  onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')}\n                   className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem]
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p>\n                       <p className=\"text-[9px] font-bold text-slate-400 uppercase tracki
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
    </div>\n              ))}\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\">\n                 <span className={`p
                  onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')}\n                   className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem]
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p>\n                       <p className=\"text-[9px] font-bold text-slate-400 uppercase tracki
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
button>\n              </div>\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\">\n                 <span className={`p
                  onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')}\n                   className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem]
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p>\n                       <p className=\"text-[9px] font-bold text-slate-400 uppercase tracki
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
/div>\n      )}\n\n      {/* ... [Rest of the activeTab blocks: CLIENTES, EQUIPE, VITRINE, ANUNCIO, PLANOS, SISTEMA] ... */}\n      {/* Kept unchanged to satisfy \"Keep updates as minimal as you can\" but 
> Gerar Com IA</button></div>\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\"><span className={`px-3 py-1 rounded-fu
ace-y-3\"><button onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')} className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem] flex items-center j
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p><p className=\"text-[9px] font-bold text-slate-400 uppercase tracking-widest mt-1\">Gestão C
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
> Gerar Com IA</button></div>\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\"><span className={`px-3 py-1 rounded-fu
ace-y-3\"><button onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')} className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem] flex items-center j
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p><p className=\"text-[9px] font-bold text-slate-400 uppercase tracking-widest mt-1\">Gestão C
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
> Gerar Com IA</button></div>\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\"><span className={`px-3 py-1 rounded-fu
ace-y-3\"><button onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')} className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem] flex items-center j
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p><p className=\"text-[9px] font-bold text-slate-400 uppercase tracking-widest mt-1\">Gestão C
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
     </div>\n              )}\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\"><span className={`px-3 py-1 rounded-fu
ace-y-3\"><button onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')} className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem] flex items-center j
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p><p className=\"text-[9px] font-bold text-slate-400 uppercase tracking-widest mt-1\">Gestão C
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
     </div>\n              )}\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\"><span className={`px-3 py-1 rounded-fu
ace-y-3\"><button onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')} className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem] flex items-center j
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p><p className=\"text-[9px] font-bold text-slate-400 uppercase tracking-widest mt-1\">Gestão C
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
     </div>\n              )}\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\"><span className={`px-3 py-1 rounded-fu
ace-y-3\"><button onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')} className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem] flex items-center j
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p><p className=\"text-[9px] font-bold text-slate-400 uppercase tracking-widest mt-1\">Gestão C
      "text": "na area de planos, o plano anunciante, nao esta mostrando o time, mesmo ele estando ativo",
tly focused on the missing timer in the \"Anunciante\" (Advertiser) plan card within the \"Plans\" (Planos) area. The user's screenshot clearly shows the \"PLANOS\" tab, but the timer component is absent, 
ly reviewing the code within it. Specifically, I'm focusing on the rendering logic related to the \"PLANOS\" tab and the conditional display of the timer component. I'm checking the current user state vari
ht not be showing its expected timer, even though it's active. My next step involves checking the \"Plano Atual\" section to see if it's the root of the timer rendering issue.\n\n\n**Isolating the Context*
f the plan activation logic, as opposed to the timer, could be linked to the missing timer in the \"PLANOS\" area. I'm focusing on the component's state to see how it affects the timer's visibility.\n\n\n*
he Timers**\n\nI'm now implementing the `currentUser.proExpiresAt` logic within the header of the \"Planos\" section, completing the timer integration. I've successfully incorporated the `PlanTimer` compon
s component and reviewing its display. I'm focused on the tabs for 'Equipe,' 'Vitrine,' 'Anuncio,' 'Planos,' and 'Sistema,' ensuring they render the correct content. I'm focusing on the `activeTab` state t
articularly focusing on the display logic for sections such as 'Equipe,' 'Vitrine,' 'Anuncio,' and 'Planos.' I'm now making sure the right content is rendered and the `activeTab` state manages rendering as
o Plan Logic**\n\nI'm now integrating the `currentUser.proExpiresAt` logic into the header of the \"Planos\" section. I am making this logic for the `PRO ATIVO` status. I'm focusing on displaying the corre
 the Display**\n\nI'm now integrating the `currentUser.proExpiresAt` logic into the header of the \"Planos\" section, completing the timer logic. This is specifically for the `PRO ATIVO` status. My focus i
r o tempo restante da assinatura ativa no card \"Anunciante\" e também no card \"Sem Ads\" na aba \"Planos\" (`components/Settings.tsx`).\n\n**Alterações Realizadas:**\n\n1.  **Novo Componente `PlanTimer`:
lanTimer` dentro dos cards de status \"Sem Ads\" e \"Anunciante\". Agora, quando o usuário tiver um plano ativo (`isAdvertiser` ou `isAdFree`), ele verá exatamente quanto tempo falta para expirar, logo aba
ixo do nome do plano.\n\nIsso resolve o problema de não mostrar o tempo mesmo com o plano ativo.\n\n",
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
     </div>\n              )}\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\"><span className={`px-3 py-1 rounded-fu
ace-y-3\"><button onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')} className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem] flex items-center j
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p><p className=\"text-[9px] font-bold text-slate-400 uppercase tracking-widest mt-1\">Gestão C
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
     </div>\n              )}\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\"><span className={`px-3 py-1 rounded-fu
ace-y-3\"><button onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')} className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem] flex items-center j
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p><p className=\"text-[9px] font-bold text-slate-400 uppercase tracking-widest mt-1\">Gestão C
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
     </div>\n              )}\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\"><span className={`px-3 py-1 rounded-fu
ace-y-3\"><button onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')} className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem] flex items-center j
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p><p className=\"text-[9px] font-bold text-slate-400 uppercase tracking-widest mt-1\">Gestão C
setActiveTab] = useState<'ESTRUTURA' | 'CLIENTES' | 'EQUIPE' | 'VITRINE' | 'ANUNCIO' | 'SISTEMA' | 'PLANOS'>('ESTRUTURA');\n  const [clientSubTab, setClientSubTab] = useState<'CLIENT' | 'SUPPLIER'>('CLIENT
l: 'Vitrine' },\n          { id: 'ANUNCIO', icon: Megaphone, label: 'Anúncios' },\n          { id: 'PLANOS', icon: CreditCard, label: 'Planos' },\n          { id: 'SISTEMA', icon: SettingsIcon, label: 'Sis
     </div>\n              )}\n           </div>\n        </div>\n      )}\n\n      {activeTab === 'PLANOS' && (\n        <div className=\"space-y-6\">\n           <div className=\"bg-slate-900 p-8 rounded
n text-white border-2 border-slate-800\">\n              <h3 className=\"text-2xl font-black mb-2\">Plano Atual</h3>\n              <div className=\"flex gap-2 mb-8\"><span className={`px-3 py-1 rounded-fu
ace-y-3\"><button onClick={() => window.open(`https://wa.me/55${'21999999999'}?text=Quero assinar o Plano PRO!`, '_blank')} className=\"w-full p-5 bg-slate-900 text-white rounded-[2rem] flex items-center j
t-black uppercase text-yellow-400 flex items-center gap-2\"><Zap size={14} fill=\"currentColor\" /> Plano PRO</p><p className=\"text-[9px] font-bold text-slate-400 uppercase tracking-widest mt-1\">Gestão C
    <button \n          onClick={() => {\n            localStorage.setItem('settings_pending_tab', 'PLANOS');\n            onNavigate('CONFIG');\n          }}\n          className=\"w-full p-6 bg-gradient-
    <button \n          onClick={() => {\n            localStorage.setItem('settings_pending_tab', 'PLANOS');\n            onNavigate('CONFIG');\n          }}\n          className=\"w-full p-6 bg-gradient-
 Configurações (Abas) e Usuários. Isso faz a tela \"montar\" quase instantaneamente.\n2.  **Segundo Plano (Dados):** Histórico de Transações, Anúncios e Dívidas Externas carregam logo em seguida, sem trava
os para renderização imediata da interface, deixando Transações (pesadas) e Anúncios para o segundo plano.",
